#include<bits/stdc++.h>
using namespace std;
int main(){
    string boxes="110";
    int n=boxes.length();
    int nofones=0;
    for(int i=0;i<n;i++){
        if(boxes[i]=='1')
        noofones++;
    }
    vector<int> result(n,0);
    for(int i=0;i<n;i++){
        
    }
    for(int i=0;i<result.size();i++){
        cout<<result[i]<<" ";
    }
}